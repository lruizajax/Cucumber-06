# Browser configuration
browser=chrome
# Browser options: chrome, firefox, edge

# Headless mode
headless=false

# Application URL
app.url=http://localhost:8080

# Timeouts (seconds)
timeout.implicit=5
timeout.explicit=10

# Parallel execution
parallel.scenarios=2
